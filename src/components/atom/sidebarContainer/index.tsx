import { tv } from "tailwind-variants";

export const SidebarContainer = tv({
  base: "bg-primary w-14 roun rounded-r-[20px] flex flex-col justify-between items-center py-[50px]",
  variants: {},
});
